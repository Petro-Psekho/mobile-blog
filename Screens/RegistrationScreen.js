import { StyleSheet, Text } from 'react-native';

export const RegistrationScreen = () => {
  return <Text style={styles.hello}>hello!</Text>;
};

const styles = StyleSheet.create({
  hello: {
    color: 'red',
    fontSize: 30,
  },
});
